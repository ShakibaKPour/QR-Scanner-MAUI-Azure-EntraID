<?xml version="1.0" encoding="UTF-8" ?>
<Shell
    x:Class="RepRepair.AppShell"
    xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
    xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
    xmlns:pages="clr-namespace:RepRepair.Pages"
    Title="RepRepair">
    <TabBar>
        <ShellContent Title="Home" Route="HomePage"  ContentTemplate="{DataTemplate pages:HomePage}" />
 
        <ShellContent Title="Scan" Route="ScanPage" ContentTemplate="{DataTemplate pages:ScanPage}" />

        <ShellContent Title="Report" Route="MainReportPage" ContentTemplate="{DataTemplate pages:MainReportPage}" />
        
    </TabBar>

</Shell>
